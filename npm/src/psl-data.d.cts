// Type surface for the generated ./psl-data.cjs (base64 of the binary PSL trie).
// The runtime file is produced from src/psl.bin by scripts/build-npm.sh and is
// kept external from the bundle so a single copy is shared by the ESM and CJS
// builds. Regenerate it; never hand-edit.
export const PSL_BASE64: string;
